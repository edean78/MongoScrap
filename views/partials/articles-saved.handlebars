<div class="row">
    <div class="col-md-12">
        <div class="card border-danger mb-3">
            <div class="card-body">
                <h4 class="card-title">{{title}}</h4>
                <p class="card-text">{{summary}}</p>
                <a href="{{url}}" target="_blank" class="card_link">{{url}}</a><br>
                <button type="button" class="btn btn-outline-danger">Remove</button>
                <button type="button" class="btn btn-outline-danger" data-toggle="notes" data-target="#modalNote{{_id}}">Add or View Notes</button>
            </div>
        </div>
    </div>
</div>