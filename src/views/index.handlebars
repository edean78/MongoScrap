{{!-- Articles --}}
{{#each article}}
<div class="row">
    <div class="col-md-12">
        <div class="card border-danger mb-3">
            <div class="card-body">
                <h4 class="card-title">{{article.title}}</h4>
                <p class="card-text">{{article.summary}}</p>
                <a href="{{article.url}}" target="_blank" class="card_link">Link</a><br>
                <button type="button" class="btn btn-outline-danger save" action="/saved/{{article._id}}" method="POST"
                    data-id={{article._id}}>Save</button>
            </div>
        </div>
    </div>
</div>
{{/each}}